# 2026-04-17 Work Log

## [00:25] v4 auto-regressive success + v5 jerk loss training + FM training improvements
**Summary:** v4 (resume v1 80k + 30k conservative autoregressive at max_rollout_prob=0.3) 成功不塌缩且修复 run prompt (1.22→0.52)。AMP-off 排查确认塌缩是算法性非数值。v3/ReFlow 塌缩确认 motion-space FM 结构性问题。启动 v5 加 3 个 FM 训练改进（jerk loss + history noise + σ_min）。Post-processing Savitzky-Golay 滤波测试将 sign_flip 从 37% → 17%。确认 V-A conditioning 不需要额外 steps。

### What was done

**v4 (方案 1: 保守 autoregressive fine-tune)**
- 从 v1 80k checkpoint resume + 30k stage 2 (max_rollout_prob=0.3)
- 结果：未塌缩 ✅，run vel 1.22→0.52 ✅，jump joint 137°→115° ✅
- K=5/K=10 测试：反而比 K=1 差（模型被校准到 K=1 误差分布）

**排查实验 (4/16)**
- AMP-off (fp32) 80k：sign_flip 28.5% vs AMP-on 32.0% → 几乎相同 → **排除 AMP 问题**
- v3 (200k stage1 + 60k stage2 + 40k stage3, max_rollout_prob=0.7)：200k 处塌缩 → rollout 0.7 也不够
- ReFlow v1-based 100k：1/8 通过 → teacher 质量不够，且 2412.08175 论文证明 ReFlow 本身有 collapse 风险
- 所有 motion-space 排查总结：算法性塌缩，非 torch/AMP/版本问题

**auto_eval.py 自动评估框架**
- `scripts/auto_eval.py` (300 行)：渲染 8 prompts + 计算 per-prompt metrics + JSON 判决
- 判据：sign_flip<0.30, max_vel<0.80, joint<155°, ≥6/8 prompts 通过
- 退出码：0=success, 2=retrain, 3=halt
- 自动建议调参（jitter 多→加 vel/acc，joint 超→加 joint_limit）

**Post-processing 测试**
- Savitzky-Golay w=9 polyorder=2 on v4 K=1 output
- sign_flip 平均 0.37→**0.17** (54% 降幅)，7/8 prompts 通过
- max_vel kick 0.38→0.18, jump 0.51→0.29

**v5 jerk loss + FM 训练改进**（当前正在训练 83%）
- 代码改动：
  - `mld/train_g1_fm.py calc_loss` 新增 jerk loss（3 阶导惩罚）weight=0.1
  - `mld/train_g1_fm.py common_step` 新增 history_noise_std=0.02（GT history 加噪）
  - `flow_matching/fm_sampler.py` 新增 sigma_min=0.001（FlowMotion 式背景噪声）
- 配置：80k stage1 + 30k stage2 @ max_rollout_prob=0.3 = 110k total
- ETA: ~6 min（83% 完成）

**文献调研 + 分析**
- TextOp 论文深度分析：确认和我们用相同 pipeline（69-dim + VAE + autoregressive），只是用 DDPM 5 步不是 FM 1 步
- FlowMotion 论文：验证 x1/x0-prediction 是正确方向（jitter 576→39.5），K=100 推理
- 论文 2412.08175：ReFlow collapse 是已知问题，real-data augmented reflow 是 fix
- 论文 pitch 明确：extend TextOp with FM (speed) + V-A (expression) + transition (smoothness)

### Problems & Solutions
- **Problem [~14:00, 4/16]:** v4 K=5/K=10 反而不如 K=1
  - **Solution:** autoregressive fine-tune 将模型校准到 K=1 误差分布。K=5 引入 train-test 不一致。若要用 K=5 推理需在 stage2 用 K=5 做 rollout。

- **Problem [~16:00, 4/16]:** v3 200k 塌缩（即使 stage1 还没结束）
  - **Solution:** 80k 是 stage1 sweet spot（欠拟合保留 diversity）。200k 过拟合到条件均值。

### Key findings
- **motion-space FM v4 recipe 最终确认**：x0-pred + GT vel/acc + jerk + joint_limit=0.3 + 80k stage1 + 30k stage2 @0.3 prob + history noise + σ_min
- **FM K=1 motion-space vs latent 各有优势**：motion 3ms / latent 6-8ms；motion 需精细调参 / latent 开箱即用
- **Post-processing Savitzky-Golay 能降 jitter 54%** 但论文写成 training 改进更 elegant
- **V-A conditioning 不需要额外 steps**（只加 ~70k 参数，2-dim 条件太简单）
- **TextOp = 我们的 DDPM baseline**，我们 extend with FM + V-A + transition

### Next steps
- [ ] 等 v5 训完（~6 min），auto_eval 对比 v4
- [ ] 开始 V-A 标注 pipeline（LLM + 运动学校准）
- [ ] 在 latent v1 上加 V-A conditioning（paper contribution #2）
- [ ] 评估 pipeline：对齐 TextOp Table III 的 FID/R@K/MM-Dist 指标
