# 2026-04-06 Work Log

## [15:50] VAE/Denoiser Training Complete + Rendering Fix

**Summary:** Both VAE and Denoiser training completed successfully on filtered data. Fixed critical rendering bug (z-offset) and test script (dof_6d→angle conversion). Added stand_g1.pkl initialization for demo.

### What was done
- Verified VAE training completed: `mvae/g1_mvae/checkpoint_300000.pt` (val rec_loss=0.00172)
- Verified Denoiser training completed: `mld_denoiser/g1_mld_v2/checkpoint_300000.pt` (val feature_rec=0.0190)
- Deleted old model checkpoints: `mvae/g1_vae_v1`, `mvae/g1_vae_v2`, `mld_denoiser/g1_mld_v1`, and all SMPL models
- Fixed `mld/test_g1_mvae.py`: replaced broken `rotation_6d_to_angle()` with `dof_6d_to_dof_pos()` using GMR's `rot_to_dof()`
- Fixed `mld/test_g1_mvae.py`: `dof_6d` is 174=29×6 (all body joints), NOT first 6=root orient — was skipping first joint and losing one
- Fixed `mld/run_g1_demo.py` and `mld/render_g1_rollout.py`: added `G1_CANON_Z_OFFSET = -0.1027` to fix ground contact
- Added stand_g1.pkl initialization in `run_g1_demo.py` for consistent standing start
- Created `mld/render_g1_rollout.py` for offline text-conditioned rollout rendering
- Fixed `diffusion/gaussian_diffusion.py`: wrapped `smpl_utils` import in try/except for G1 pipeline
- Configured Notion MCP in Claude Code
- Created `/log-notion` skill with Notion integration

### Problems & Solutions
- **Problem [15:00]:** `test_g1_mvae.py` rendered distorted/twisted poses
  - **Solution:** `rotation_6d_to_angle()` was wrong — used rotvec magnitude approximation. Replaced with `dof_6d_to_dof_pos()` using GMR's `KinematicsModel.rot_to_dof()` for exact inverse. Also fixed: code skipped first 6 dims of `dof_6d` as "root orient" but `dof_6d` is all 29 body joints (root orient is in `global_orient_delta_6d`).
- **Problem [15:20]:** Robot floating ~10cm above ground in rendering
  - **Solution:** Canonicalization shifts `transl_z` by the left_hip_pitch_link offset (-0.1027). Added constant `G1_CANON_Z_OFFSET = -0.1027` to all rendering code. Verified: without offset foot_z=0.123, with offset foot_z=0.020 (ground contact).
- **Problem [15:30]:** Demo starts from random pose, not standing
  - **Solution:** Added code to load `stand_g1.pkl`, convert to canonicalized features, normalize, and use as initial history frames.
- **Problem [15:35]:** Denoiser crashed on import `from utils.smpl_utils import get_smplx_param_from_6d`
  - **Solution:** Wrapped import in `try/except` in `diffusion/gaussian_diffusion.py` — function only used in SMPL pipeline.
- **Problem [15:45]:** Notion MCP not loading in Claude Code
  - **Solution:** MCP was configured in Cursor, not Claude Code. Added with `claude mcp add -e NOTION_TOKEN=... notion npx -y @notionhq/notion-mcp-server`.

### Key findings
- VAE rec_loss=0.00172 (filtered data) vs 0.000099 (previous unfiltered) — slightly higher but expected with fewer samples
- Denoiser val feature_rec=0.0190, latent_rec=0.115 — train/val gap small, no overfitting
- `G1_CANON_Z_OFFSET = -0.1027` is constant across all training data (left_hip_pitch_link z relative to root)
- Walk locomotion still limited by DART's 10-frame primitive design — needs RL steering (Phase 5)

### Next steps
- Test demo on main display machine
- Investigate walk/locomotion quality improvement
- Move to Phase 5: RL steering policy
- Consider transferring to Isambard for faster iteration

## [16:12] Notion Integration & Skill Setup

**Summary:** Configured Notion MCP, created `/log-notion` skill, iterated on Notion formatting (toggle, to_do, heading blocks), fixed Research Log to use shared daily page with project headings.

### What was done
- Configured Notion MCP server in Claude Code: `claude mcp add -e NOTION_TOKEN=... notion npx -y @notionhq/notion-mcp-server`
- Discovered Notion MCP tool only supports `paragraph` and `bulleted_list_item` blocks — used `curl` to Notion REST API for `heading`, `to_do`, `toggle`, `code`, `divider`
- Created `/log-notion` skill at `.claude/skills/log-notion/SKILL.md`
- Created Notion VA_MoGen Experiments entry (`33a2d672-a3d2-81b8-972f-fbad1c4dfc03`) with TODO checkboxes + toggle Problems & Solutions
- Created Notion Research Log entry (`33a2d672-a3d2-8194-875e-cd0f48776bec`) — restructured as shared daily page with `heading_1` per project
- Iterated on formatting: first attempt was ugly (plain paragraphs), second attempt used proper block types

### Problems & Solutions
- **Problem [15:45]:** Notion MCP tools not loaded — MCP was in Cursor config, not Claude Code
  - **Solution:** `claude mcp add -e NOTION_TOKEN=ntn_... notion npx -y @notionhq/notion-mcp-server`, then reload window
- **Problem [15:52]:** MCP `patch-block-children` only supports `paragraph` and `bulleted_list_item` — TODO/toggle/heading not available
  - **Solution:** Used `curl` to Notion REST API directly for all block types (`heading_2`, `to_do`, `toggle`, `code`, `divider`)
- **Problem [16:00]:** Research Log was created as project-specific entry, but should be shared daily page with project headings
  - **Solution:** Updated skill Step 4 — query for today's page (any project), find/create `heading_1: VA_MotionGeneration` section, append under it

### Key findings
- Notion MCP `post-page` and `patch-page` tools have limited property support — use curl for Date, Select, RichText properties
- Notion REST API supports nested blocks (toggle with children) in a single request
- Research Log DB (`3382d672-a3d2-81e6-8b76-d4c9e6cace5e`) — shared, one page per day, project sections via heading_1
- Experiments DB (`3382d672-a3d2-8194-8bb8-d5810a56257f`) — per-experiment entries under VA_MoGen page

### Next steps
- Verify demo rendering on display machine
- Phase 5: RL steering policy
