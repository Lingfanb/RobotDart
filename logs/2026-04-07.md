# 2026-04-07 Work Log

## [00:16] Weighted Sampling + Code Cleanup + Denoiser v3 Training

**Summary:** Diagnosed why denoiser generates same motion for all prompts (text conditioning failure). Root cause: uniform sampling + CLIP low discriminability. Added inverse-frequency weighted sampling. Cleaned up duplicate code. Started denoiser v3 training.

### What was done
- Diagnosed denoiser text conditioning failure:
  - Generated "kick" vs "stand" have identical leg joints (knee ~0.1 rad vs GT kick knee ~2.3 rad)
  - CLIP embedding cosine similarity between all prompts is 0.75-0.88 — too similar
  - "stand" dominates training data at 10.8% (7167/66496 primitives)
- Compared SMPL vs G1 dataset pipeline:
  - SMPL uses `action_statistics.json` + weighted sampling (`np.random.choice(p=seq_weights)`)
  - G1 uses uniform `random.choices()` — no balancing
- Added weighted sampling to `data_loaders/humanml/data/dataset_g1.py`:
  - Inverse text frequency: `weight = 1/count(text)` per primitive
  - stand: 8.1% → 1.5%, kick: 0.08% → 0.11% (1.8x boost)
  - Updated `train_g1_mld.py` and `train_g1_mvae.py` to use `weight_scheme='text'`
- Code cleanup (`/simplify`):
  - Moved `G1_CANON_Z_OFFSET`, `dof_6d_to_qpos()`, `set_mujoco_from_features()` to `utils/g1_utils.py`
  - Removed duplicate functions from `run_g1_demo.py`, `render_g1_rollout.py`, `test_g1_mvae.py`
  - Removed dead code (unused stand_g1.pkl loading, unused imports)
- Fixed `run_g1_demo.py` stand initialization — use dataset "stand" sample instead of broken stand_g1.pkl conversion
- Started denoiser v3 training with weighted sampling on GPU 1 (batch_size=4096)
- Configured Lark MCP globally in Claude Code
- Updated `/log-notion` skill: removed Research Log step

### Problems & Solutions
- **Problem [22:00]:** Denoiser generates identical motion for all text prompts
  - **Solution:** Root cause is uniform sampling + CLIP similarity. Added inverse-frequency weighted sampling to `dataset_g1.py`. Need to retrain denoiser.
- **Problem [22:30]:** stand_g1.pkl initialization caused robot to fly
  - **Solution:** Rolled back to using a "stand" sample from training dataset instead of manual feature construction from stand_g1.pkl.
- **Problem [23:00]:** Duplicate code across 4 rendering scripts
  - **Solution:** Consolidated `dof_6d_to_qpos`, `set_mujoco_from_features`, `G1_CANON_Z_OFFSET` into `utils/g1_utils.py`. All scripts import from there.

### Key findings
- CLIP ViT-B/32 cosine similarity: stand↔kick=0.855, stand↔walk=0.852 — almost indistinguishable
- SMPL DART uses action-weighted sampling that G1 was missing — this is the primary cause of text conditioning failure
- Guidance scale (5→30) has minimal effect when model hasn't learned text-action mapping
- Weighted sampling changes: stand 8.1%→1.5%, rare actions get 1.5-2x boost

### Next steps
- Wait for denoiser v3 training to complete (~12-15h with batch_size=4096)
- Generate comparison videos: v2 (uniform) vs v3 (weighted)
- If still insufficient, consider: longer primitives, better text encoder, or data augmentation
