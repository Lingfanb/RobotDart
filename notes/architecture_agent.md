# VADBridge Architecture — LLM Agent + Tool-Use

**Paradigm**: OpenClaw-inspired autonomous agent. LLM as brain, perception/skill modules as tools, ReAct loop (Reason → Act → Observe).

---

## 1. Full Architecture

```
┌────────────────────────────────────────────────────────────────┐
│                      PHYSICAL WORLD                             │
│     User (face, voice, body)   Objects (cups, etc.)    Scene   │
└─────────────┬─────────────────────┬──────────────────────┬─────┘
              │                     │                      │
         ┌────▼────┐           ┌────▼────┐            ┌────▼────┐
         │ Camera  │           │  Mic    │            │RealSense│
         └────┬────┘           └────┬────┘            └────┬────┘
              │                     │                      │
 ═════════════╪═════════════════════╪══════════════════════╪══════
              ▼                     ▼                      ▼
 ┌────────────────────────────────────────────────────────────────┐
 │                 PERCEPTION TOOLS (EYES)                        │
 │                                                                │
 │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
 │  │ P-Face   │  │ P-Voice  │  │ P-Body   │  │ P-Object │        │
 │  │ face VAD │  │voice VAD │  │ action   │  │ 6DOF     │        │
 │  │          │  │ + ASR    │  │ + pose   │  │ pose     │        │
 │  └─────┬────┘  └─────┬────┘  └─────┬────┘  └─────┬────┘        │
 │        │             │             │             │              │
 │        └─────────────┴─────────────┴─────────────┘              │
 │              structured state snapshot (JSON)                   │
 └────────────────────────┬───────────────────────────────────────┘
                          │      tool_result
                          ▼
 ═════════════════════════╪══════════════════════════════════════
                          │
 ┌────────────────────────▼───────────────────────────────────────┐
 │                         M-BRAIN                                │
 │          LLM Agent (Claude/GPT-4o, prompt-only, no tuning)     │
 │                                                                │
 │     ┌────────────────────────────────────────────────────┐     │
 │     │       ReAct Loop: Reason → Act → Observe           │     │
 │     │                                                    │     │
 │     │   ┌─── Reason ◄── History + current state          │     │
 │     │   │                                                │     │
 │     │   ▼                                                │     │
 │     │   Act: tool_call ──► perception / skill / output   │     │
 │     │   │                                                │     │
 │     │   ▼                                                │     │
 │     │   Observe ──► tool_result                          │     │
 │     │   │                                                │     │
 │     │   └──► loop back                                   │     │
 │     └────────────────────────────────────────────────────┘     │
 │                                                                │
 │   Decisions emit: skill_id + VAD target + motion text +        │
 │                   target_object + phase + verbal response      │
 └─────────┬─────────────────────────┬────────────────────────────┘
           │                         │
           │  tool_call              │  tool_call
           ▼                         ▼
 ═════════════════════════╪══════════════════════════════════════
                          │
 ┌────────────────────────▼───────────────────────────────────────┐
 │                    SKILL TOOLS (HANDS)                         │
 │                                                                │
 │    ┌─────────────────────┐    ┌─────────────────────┐          │
 │    │      S-Motion       │    │      S-Manip        │          │
 │    │                     │    │                     │          │
 │    │  FM (VAD-cond)      │    │  FM (VAD + object   │          │
 │    │  text + VAD +       │    │  + phase + text)    │          │
 │    │  history → motion   │    │  → handover motion  │          │
 │    └──────────┬──────────┘    └──────────┬──────────┘          │
 │               │                          │                     │
 │               └────────────┬─────────────┘                     │
 │                            │                                   │
 │                 69-dim motion primitive                        │
 └────────────────────────────┬───────────────────────────────────┘
                              │
 ═════════════════════════════╪═══════════════════════════════════
                              │
 ┌────────────────────────────▼───────────────────────────────────┐
 │                     OUTPUT TOOLS (BODY)                        │
 │                                                                │
 │    ┌─────────────────────┐        ┌─────────────────────┐      │
 │    │     O-Robot         │        │      O-Voice        │      │
 │    │                     │        │                     │      │
 │    │  motion primitive → │        │  text → TTS →       │      │
 │    │  G1 joint targets   │        │  speaker audio      │      │
 │    │  + safety filter    │        │                     │      │
 │    └──────────┬──────────┘        └──────────┬──────────┘      │
 └───────────────┼──────────────────────────────┼────────────────┘
                 ▼                              ▼
           ┌──────────┐                    ┌──────────┐
           │  G1 body │                    │ Speaker  │
           │  joints  │                    │          │
           └──────────┘                    └──────────┘
```

---

## 2. Module List

### Layer 1: Brain (1 module)
| ID | Name | Role |
|---|---|---|
| **M-Brain** | LLM Agent | 中枢决策，ReAct loop，prompt-only |

### Layer 2: Perception Tools (4 modules)
| ID | Name | Role | Input | Output |
|---|---|---|---|---|
| **P-Face** | Face VAD | 从脸读情感 | camera image | VAD (face) |
| **P-Voice** | Voice VAD + ASR | 从声音读情感 + 识别话 | mic audio | VAD (voice) + text |
| **P-Body** | Action + Pose | 识别用户姿态+动作 | camera image | action class + 3D pose |
| **P-Object** | Object 6DOF | 场景中物体位置 | depth + camera | object list + poses |

### Layer 3: Skill Tools (2 modules)
| ID | Name | Role | Input | Output |
|---|---|---|---|---|
| **S-Motion** | Expressive Motion | 走路/手势/表达 | text + VAD + history | 69-dim primitive |
| **S-Manip** | Social Handover | 物体递/接 | text + VAD + history + object + phase | 69-dim primitive |

### Layer 4: Output Tools (2 modules)
| ID | Name | Role | Input | Output |
|---|---|---|---|---|
| **O-Robot** | Robot Controller | 69-dim → 关节命令 + 安全 | motion primitive | G1 joint targets |
| **O-Voice** | TTS | 话术 → 音频 | text | speaker audio |

---

## 3. Agent Loop (ReAct)

```
loop {
  state = { P-Face, P-Voice, P-Body, P-Object }  ← continuously updated in background

  M-Brain.reason(state, history)
       │
       ▼
  decision = M-Brain.act() ──► tool_call {
      skill_id: "motion" | "manip",
      params: { text, VAD, object, phase, ... },
      verbal: "..."
  }
       │
       ▼
  execute tool ──► observe result
       │
       ▼
  loop
}
```

Trigger to enter loop: **user utterance end** / **user VAD change** / **skill completion** / **timeout**.

---

## 4. Module Count Summary

- **1 Brain** + **4 Perception** + **2 Skill** + **2 Output** = **9 modules total**

Categorized by novelty:
- **Core research contribution** (new work): M-Brain, S-Motion (VAD-cond), S-Manip
- **Systems integration** (wrap pretrained): P-Face, P-Voice, P-Body, P-Object, O-Voice
- **Hardware integration**: O-Robot

---

## 5. External Interface

**Input devices**:
- Camera (RGB)
- RealSense (RGB-D)
- Microphone array
- G1 joint encoders (robot state)

**Output devices**:
- G1 29-DOF body + hand
- Speaker

**External LLM API** (for M-Brain):
- Claude API (Anthropic) — primary
- OpenAI API — fallback
- Local LLM (Qwen-VL etc.) — extended version
